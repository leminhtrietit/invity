"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export function AccountDeletionForm(){const router=useRouter();const [confirmation,setConfirmation]=useState("");const [message,setMessage]=useState("");const [saving,setSaving]=useState(false);async function submit(event:React.FormEvent){event.preventDefault();setSaving(true);const response=await fetch("/api/v1/account/deletion",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({confirmation})});const body=await response.json().catch(()=>null);if(response.ok){router.push("/");router.refresh();return}setSaving(false);setMessage(body?.error?.message??"Chưa thể gửi yêu cầu.")}return <form className="danger-zone-form" onSubmit={submit}><label><span>Nhập <strong>XOA TAI KHOAN</strong> để xác nhận</span><input autoComplete="off" value={confirmation} onChange={(event)=>setConfirmation(event.target.value)}/></label><button disabled={saving||confirmation!=="XOA TAI KHOAN"} type="submit">{saving?"Đang khóa truy cập…":"Xóa tài khoản"}</button>{message&&<p role="alert">{message}</p>}</form>}
