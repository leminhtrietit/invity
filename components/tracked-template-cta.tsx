export function TrackedTemplateCta({templateId}:{templateId:string}){return <a className="button button-primary" data-template-selection={templateId} href={`/login?returnTo=${encodeURIComponent(`/dashboard?templateId=${templateId}`)}`}>Dùng mẫu này</a>}
