"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export function WaitlistForm({signedIn}:{signedIn:boolean}){const router=useRouter();const [plan,setPlan]=useState<"pro"|"premium">("pro");const [consent,setConsent]=useState(false);const [message,setMessage]=useState("");const [saving,setSaving]=useState(false);async function submit(event:React.FormEvent){event.preventDefault();if(!signedIn){router.push("/login?returnTo=%2Fpricing");return}setSaving(true);const response=await fetch("/api/v1/waitlist",{method:"POST",headers:{"Content-Type":"application/json","Idempotency-Key":crypto.randomUUID()},body:JSON.stringify({planInterest:plan,consent})});const body=await response.json().catch(()=>null);setSaving(false);setMessage(response.ok?"Đã giữ chỗ. Chúng tôi sẽ báo khi gói này sẵn sàng.":body?.error?.message??"Chưa thể giữ chỗ.")}return <form className="waitlist-form" onSubmit={submit}><label><span>Gói bạn quan tâm</span><select value={plan} onChange={(event)=>setPlan(event.target.value as "pro"|"premium")}><option value="pro">Pro</option><option value="premium">Premium</option></select></label><label className="waitlist-consent"><input checked={consent} onChange={(event)=>setConsent(event.target.checked)} type="checkbox"/><span>Tôi đồng ý nhận email khi gói trả phí mở đăng ký. Đây là consent riêng và có thể rút lại qua kênh hỗ trợ.</span></label><button className="button button-primary" disabled={!consent||saving} type="submit">{saving?"Đang ghi nhận…":signedIn?"Giữ chỗ nâng cấp":"Đăng nhập để giữ chỗ"}</button>{message&&<p role="status">{message}</p>}</form>}
